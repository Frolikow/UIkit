include ../stages/stages

mixin stages-container
    .stages-container
        +title('stages')
        .stages-container__stages
            +stages({
                length: 5, 
                value: 3
            })
