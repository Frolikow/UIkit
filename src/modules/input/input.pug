mixin input({type, placeholder, name})
    .input
        input(type = `${type}` placeholder = placeholder class = `input__element input__element_validation` required name = name)
        .input__info